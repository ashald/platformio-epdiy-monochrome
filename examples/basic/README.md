# What

A primitive example showing basic usage of the library - see [`main.cpp`](src/main.cpp).

# Run
1. Adjust `env` in `platformio.ini` as/if needed
2. `pio run -t upload `
3. `pio device monitor`